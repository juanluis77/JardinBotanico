
/*const functions = require('firebase-functions');
const admin = require('firebase-admin');
admin.initializeApp();

// Exporta tus funciones de Firebase
exports.miFuncion = functions.https.onRequest((request, response) => {
  // Tu lógica de función aquí
  response.status(200).send('¡Hola desde Firebase!');
});*/
