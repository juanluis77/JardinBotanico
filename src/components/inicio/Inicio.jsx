// Inicio.jsx
import React from 'react';
import MainContent from './MainContent';

function Inicio() {
  // Contenido específico de la página de inicio
  const inicioContent = (
    <div>
      {/* Contenido de inicio */}
    </div>
  );

  return <MainContent>{inicioContent}</MainContent>;
}

export default Inicio;